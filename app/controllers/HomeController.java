package controllers;

import models.LoginDetails;
import play.data.FormFactory;
import play.mvc.*;
import javax.inject.Inject;
import views.*;


public class HomeController extends Controller {

    


    // public Result home() {
    //     return ok(views.html.home.render(loginDetails.getName(), loginDetails.getPassword(), asScala(sidemenu)));
    // }

    // public Result login() {
    // 	int i=0;
    //     return ok(views.html.izlogin.render());
    // }

    // // Handle GET /home with a parameter
    // public Result testPost(String name) {
    //     return ok(views.html.name.render(name));
    // }

    // // Handle POST /login
    // public Result loginPost() {
    //     String uname = "Guna";
    //     // Redirect to testPost with the username as a parameter
    //     return redirect(routes.HomeController.testPost(uname));
    // }

    // Inner class SampleDependencyconst should be moved outside HomeController for correct syntax

}
